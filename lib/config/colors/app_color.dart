import 'dart:ui';

import 'package:flutter/material.dart';

class AppColor {
  // light theme colors
  static const Color lightPrimary = Colors.blue;
  static const Color whiteColor = Colors.white;
  static const Color blackColor = Colors.black;

  // dark theme colors
  static const Color darkPrimary = Color.fromARGB(255, 219, 213, 213);
  static const Color darkAcent = Colors.green;

  // custom button colors
    static const Color lightTheme = Colors.orange; 
  static const Color darkTheme = Color(0xFF3949AB);  
  static const Color systemTheme = Color(0xFF66BB6A); 

}